var assert = require('assert');

context('Test Suite: config.js unit tests', function {

describe('Command Line Args', function {
    it('should start with no errors if no command line args', function () {

    })
});


});
